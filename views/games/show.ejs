<%- include('../partials/header') %>

    <header><a href="/games">Games List</a></header>
    <header><a href="/games/<%= gameDoc._id %>/edit">Edit Game</a></header>
    <h1><%= gameDoc.name %>, <%= gameDoc.releaseYear %></h1>
    <% if (gameDoc.isCompleted) { %>
        <p><%= gameDoc.name %> has been completed! Congratulations!</p>
        <% } else { %>
         <p><%= gameDoc.name %> has not yet been completed.</p>
    <% } %>
    <form action="/games/<%=gameDoc._id%>?_method=DELETE" method="POST">
        <button>Delete Game</button>
    </form>

<%- include('../partials/footer') %>